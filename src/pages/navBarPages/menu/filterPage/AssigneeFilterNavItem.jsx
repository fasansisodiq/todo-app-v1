import UniqueAssignees from "../../../../utils/UniqueAssignees";

function AssigneeFilterNavItem({ onClick }) {
  return <UniqueAssignees onClick={onClick} />;
}

export default AssigneeFilterNavItem;
